# API URL
VITE_API_URL=http://localhost:5000/api

# WhatsApp Number (with country code, no +)
VITE_WHATSAPP_NUMBER=919562313752

# Admin secret — must match ADMIN_SECRET in backend .env
# Used by admin pages to call /api/guest-admin/* routes
VITE_ADMIN_SECRET=change_this_to_a_strong_random_secret

